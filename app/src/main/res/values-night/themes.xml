<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.LocalFoodPrototip" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <!-- Status bar color. -->
        <item name="colorPrimary">@color/vermelho</item>
        <item name="colorSecondary">@color/laranja</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:statusBarColor">@color/vermelho</item>
        <!-- Customize your theme here. -->
    </style>
</resources>